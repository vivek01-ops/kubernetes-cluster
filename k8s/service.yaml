apiVersion: v1
kind: Service
metadata:
  name: nodeapp-service
spec:
  type: NodePort
  selector:
    app: nodeapp
  ports:
  - port: 3000       # Port inside the cluster
    targetPort: 3000 # Port on the Pod
    nodePort: 30080  # Port exposed on the Minikube node (must be between 30000-32767)
